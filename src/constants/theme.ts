import { DefaultTheme } from "styled-components/native";

export const palette = {
  pink: "#FFB5B5",
  pink_sub: "#FFE2E2",
  black: "#000000",
  white: "#FFFFFF",
} as const;

export const THEME = {
  palette: palette,
} as const;
